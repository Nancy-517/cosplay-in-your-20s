
# 任务：重做小游戏前端体验，不要再做成黑色卡片问卷

## 当前问题

第二版虽然比第一版规整了一些，但仍然像一个暗黑后台面板，而不是一个有趣、生动、沉浸的互动小游戏。

当前问题包括：

1. 背景仍然只是黑色渐变，没有丰富的视觉变化。
2. 剧情节点没有真正场景化，还是大段文字卡片。
3. 没有微信聊天框、电脑界面、文件资料、邮箱、文档批注等具体视觉元素。
4. 角色卡不够像剧本杀角色档案。
5. 选项仍然像表单按钮，不像游戏里的行动选择。
6. 关系图谱有了，但整体仍然很静态。
7. 页面缺少小游戏的趣味、动效、场景转换和视觉记忆点。

请不要再只做“暗黑卡片 + 关系图谱 + timeline”。这次要把 `/play` 和 `/character` 真正重构成“场景化互动剧本杀小游戏”。

---

## 一、整体目标

把当前体验从：

“暗黑问卷页面”

改成：

“校园信息战互动剧本杀小游戏”。

整体风格关键词：

- 微信聊天
- 电脑桌面
- 邮件窗口
- PDF 文件
- 资料夹
- 文档批注
- 群聊传言
- 日程倒计时
- 角色档案
- 关系线
- 信息差
- 青年成长
- 轻悬疑
- 有戏剧感但不幼稚

请使用 CSS、HTML、SVG、React 组件做出这些界面，不要依赖真实图片资源。

---

## 二、重做背景：不要纯黑，要有“信息战桌面感”

当前背景太单调。

请重做全局背景，要求：

1. 使用多层 radial-gradient，让背景有冷蓝、深紫、暗红、墨绿的微弱光区。
2. 增加细网格背景，像电脑系统或情报面板。
3. 增加微弱噪点纹理，可以用 CSS pseudo-element 实现。
4. 增加几条半透明关系线或数据线，在背景中缓慢漂浮。
5. 增加少量浮动元素，例如：
   - 半透明文件卡片
   - 模糊聊天气泡
   - 小型光点
   - 扫描线
6. 不要花哨到影响阅读，但要明显比现在更有设计感。

请修改：

- `src/app/globals.css`
- 或现有全局样式文件

建议增加 class：

- `.app-background`
- `.ambient-grid`
- `.noise-overlay`
- `.floating-clues`
- `.scanline`

---

## 三、重做角色卡页：要像“剧本杀开局角色档案”

当前角色卡不够有仪式感。请把 `/character` 重做成角色档案页面。

角色卡页结构：

```txt
左侧：玩家身份档案
右侧：关系人物档案
底部：开始进入局中按钮
```

视觉要求：

### 左侧玩家档案

做成一张“档案袋 / 学生档案卡”：

- 标题：你的角色档案
- 角色名：你
- 身份：绩点稳定、正在准备保研 / 考研关键节点的学生
- 表面目标：拿到目标导师认可，争取夏令营或复试机会
- 隐藏压力：你害怕自己一直相信的努力和体面，在真正的资源竞争面前并不够用
- 初始资源：
  - 往届面试资料线索
  - 学长学姐信息渠道
  - 稳定但并非绝对领先的绩点
  - 一个原本约定共享信息的盟友

视觉元素：

- 档案编号
- “CONFIDENTIAL” 或 “仅本人可见”印章
- 纸张纹理
- 夹子 / 标签页效果
- 初始五维状态条

### 右侧 NPC 档案

四个 NPC 做成可翻阅的小档案卡：

1. 林亦然：你的备考盟友
2. 周启明：普通同学 / 竞争者
3. 许知夏：消息灵通的同学
4. 沈砚：可以咨询的前辈

每张卡片显示：

- 姓名
- 表面身份
- 你已知的信息
- 未知动机用 `???` 或模糊遮罩表示

动画要求：

- 玩家档案从左侧滑入
- NPC 档案逐张翻开
- “开始进入局中”按钮有轻微呼吸光

---

## 四、重做 `/play`：每个节点必须是不同的场景 UI

当前所有节点仍然像文字卡片。请新增或重构：

- `src/components/SceneFrame.tsx`

要求：根据当前剧情节点 id，渲染不同的场景化界面。

不要只显示 sceneText。sceneText 可以保留，但必须和场景 UI 结合。

---

# 节点 1：那份资料

## 场景 UI：电脑桌面 + PDF 文件掉落

节点 id 可能是 `node-1`。

请做成一个电脑桌面界面：

```txt
桌面背景
左侧几个文件夹
中间一份 PDF 文件卡片
右上角显示“下载完成”
文件名：往届面试资料_未公开.pdf
标签：高频问题 / 导师偏好 / 非公开提醒
```

动画：

1. PDF 文件卡片从上方轻微掉落。
2. 文件名闪一下。
3. 三个标签依次出现。
4. 出现一行提示：“这份资料不算决定胜负，却足够让人提前知道门是在哪一侧开的。”

组件样式建议：

- 类似 Mac / Windows 桌面窗口
- 文件图标
- PDF 红色角标
- 资料高亮条

---

# 节点 2：模糊暗示

## 场景 UI：微信 / 私信聊天框

节点 id 可能是 `node-2`。

请做成一个手机聊天界面或微信聊天窗口。

聊天对象：沈砚

聊天内容示例：

```txt
你：学长/学姐，想请教一下今年导师组的方向和名额情况。
沈砚：今年名额可能会比较紧。
沈砚：老师会更看重提前沟通过、方向比较贴的人。
沈砚：不过具体情况我也不太方便说死。
```

视觉要求：

- 类微信聊天框
- 左右气泡
- 顶部显示“沈砚”
- 状态：在线 / 刚刚活跃
- “对方正在输入...” 动画
- 关键句“名额可能会比较紧”高亮

动画：

1. 你的消息先出现。
2. typing indicator 出现。
3. 沈砚消息逐条出现。
4. 关键句轻微闪烁或标黄。

---

# 节点 3：盟友沉默

## 场景 UI：微信聊天 + 已读不回

节点 id 可能是 `node-3`。

聊天对象：林亦然

显示历史聊天：

```txt
你：最近有看到新的夏令营经验帖吗？
林亦然：我也还没怎么看。
你：你昨天是不是和目标院校的学姐聊过？
林亦然：啊，就是随便问问。
```

视觉要求：

- 类微信聊天框
- 出现“对方正在输入...”
- 然后 typing 消失
- 出现短句回复
- 有已读感，但不直接写“已读”

动画：

1. 历史消息逐条浮现。
2. typing indicator 出现 1 秒后消失。
3. 最后一条回复延迟出现。
4. 聊天窗口边缘有轻微冷光，营造不安感。

---

# 节点 4：体面求助

## 场景 UI：Word / 文档批注界面

节点 id 可能是 `node-4`。

请做成一个文档编辑器界面。

文件名：

```txt
个人陈述_周启明.docx
```

文档正文片段：

```txt
我长期关注该方向，并曾围绕相关议题完成课程论文与项目整理……
```

右侧批注：

```txt
批注 1：研究方向和你高度重合
批注 2：他已经写进了导师相关经历
批注 3：这不是刚开始准备的人
```

视觉要求：

- 文档白纸或深色文档界面均可
- 右侧批注气泡
- 光标闪烁
- 几处黄色高亮
- 顶部工具栏模拟文档软件

动画：

1. 文档窗口展开。
2. 批注线逐条出现。
3. 关键句被高亮。
4. 光标停在某一段文字后闪烁。

---

# 节点 5：最后入口

## 场景 UI：日程邀请 + 倒计时 + 门缝光

节点 id 可能是 `node-5`。

请做成一个日历 / 日程邀请界面。

内容：

```txt
导师沟通窗口
明日 15:30
时长：不确定
可安排人数：不确定
来源：沈砚
```

视觉要求：

- 日历卡片
- 倒计时模块
- “确认沟通”按钮样式但不可点击，只作为视觉
- 一条像门缝一样的光线
- 显示“最后入口”感

动画：

1. 日程卡片从右侧滑入。
2. 倒计时数字轻微跳动。
3. 门缝光线 pulse。
4. 页面氛围偏紧张。

---

# 节点 6：结果前夜

## 场景 UI：群聊传言流 / 信息噪声

节点 id 可能是 `node-6`。

请做成一个群聊刷屏界面。

群名：

```txt
夏令营互助群
```

消息示例：

```txt
听说今年名额比想象中少……
有人是不是早就联系过了？
导师是不是已经有倾向人选了？
别乱说吧，也不一定。
反正有人动作挺快的。
```

视觉要求：

- 群聊气泡快速出现
- 一些关键词模糊或 glitch
- 背景有噪点
- 信息流滚动
- 让用户感觉传言正在扩散

动画：

1. 消息逐条快速浮现。
2. 关键词“名额”“倾向人选”“早就联系过”闪烁。
3. 某些消息半透明，像传言。
4. 最后停留在当前剧情正文。

---

## 五、剧情正文与场景 UI 的关系

每个节点页面不要只有大段正文。

建议结构：

```txt
上方：地图信息 + 节点进度
中间左侧：场景化 UI
中间右侧：关系图谱 / 多方动态
下方：当前剧情正文 + 行动卡
```

或者：

```txt
左侧：场景化 UI
右侧：剧情正文 + 行动卡
下方：关系图谱
```

请根据现有布局选择最适合实现的方案，但必须保证：

1. 场景 UI 是视觉重点。
2. 剧情正文不再是唯一内容。
3. 行动选择像游戏动作，而不是问卷选项。

---

## 六、选项改成“行动卡牌”，加卡片甩出效果

当前选择仍然像普通按钮。

请新增或重构：

- `src/components/ActionCard.tsx`

视觉要求：

1. 三个选项像三张行动卡。
2. 桌面端可以轻微错位或扇形排列。
3. hover 时卡片上浮、边缘发光。
4. 点击时卡片向前放大，其他卡片淡出。
5. 卡片上显示：
   - 行动标题
   - 风险标签
   - 可能影响的维度

风险标签可以根据 effects 自动生成：

- resource 大幅增加：资源收益
- resource 大幅减少：资源代价
- trust 大幅增加：信任上升
- trust 大幅减少：信任风险
- boundary 大幅减少：边界代价
- anxiety 大幅增加：压力上升
- reputation 大幅增加：声望收益

点击后显示后果浮层。

---

## 七、选择后果浮层要更像“系统结算”

当前不要直接跳下一题。

请新增或重构：

- `src/components/ChoiceConsequenceModal.tsx`

浮层内容：

```txt
你选择了：XXX

选择后果：
[feedback 文案]

状态变化：
资源 +10
信任 -4
声望 +0
焦虑 +6
边界 -5

多方反应：
林亦然：……
周启明：……
许知夏：……
沈砚：……

按钮：继续
```

视觉要求：

- 像系统弹出的“后果结算卡”
- 背景轻微模糊
- 状态变化逐项出现
- NPC 反应逐条出现
- 点击继续后，当前场景卡片滑出，下一节点滑入

---

## 八、右侧关系图谱继续保留，但要更有生命力

当前关系图谱已经有雏形，但还是静态。

请增强：

1. 中间“你”可以改成更明显的玩家核心节点。
2. NPC 节点用中文姓名为主，不要只写 A/B/C/D。
3. 连线根据态度值有动态颜色和粗细。
4. 每次选择后，变化最大的关系线 pulse。
5. 节点 hover 时显示角色小档案：
   - 表面身份
   - 当前态度
   - 当前行动
6. 关系图谱下面只保留简洁 timeline，不要重复长文字。

---

## 九、结局页也要像“系统生成报告”，不要静态展示

当前结局页仍然像普通结果页。

请增强：

1. 进入结果页先出现 3 秒左右的结算动画。
2. 文案逐条出现：

```txt
正在回收你的选择记录...
正在重建关系网络...
正在计算隐藏代价...
正在生成平行人生...
```

3. 视觉上出现：
   - 进度条
   - 扫描线
   - 关系图谱重组
   - 五维状态快速回放
4. 结果卡逐项 reveal：
   - 最终结算
   - 结局名称
   - 人格标签
   - 关系结算
   - 隐藏代价
   - 平行人生

请新增或重构：

- `src/components/SettlementLoading.tsx`

---

## 十、不要生成真实图片，用前端组件模拟

请不要依赖外部图片资源。

不要去调用图片生成模型。

请使用：

- CSS
- HTML
- SVG
- React components
- pseudo elements
- gradient
- animation

模拟以下视觉元素：

- 微信聊天框
- 手机窗口
- PDF 文件
- 电脑桌面
- Word 文档批注
- 日程卡片
- 群聊传言流
- 角色档案卡
- 关系图谱

---

## 十一、建议修改文件

请优先检查并修改以下文件：

- `src/app/play/PlayClient.tsx`
- `src/app/character/page.tsx`
- `src/app/result/ResultClient.tsx`
- `src/components/RelationshipPanel.tsx`
- `src/components/StatusBar.tsx`
- `src/components/ChoiceButton.tsx`
- `src/components/ResultCard.tsx`
- `src/app/globals.css`

建议新增：

- `src/components/SceneFrame.tsx`
- `src/components/ActionCard.tsx`
- `src/components/ChoiceConsequenceModal.tsx`
- `src/components/SettlementLoading.tsx`
- `src/components/CharacterDossier.tsx`
- `src/components/ChatScene.tsx`
- `src/components/DesktopFileScene.tsx`
- `src/components/DocumentReviewScene.tsx`
- `src/components/CalendarWindowScene.tsx`
- `src/components/RumorFeedScene.tsx`

如果你认为可以合并组件，也可以合并，但最终必须实现六个节点的不同视觉场景。

---

## 十二、验收标准

完成后请逐项检查：

1. `/character` 页面像剧本杀角色档案，而不是普通卡片。
2. `/play` 节点 1 显示电脑桌面 + PDF 文件。
3. `/play` 节点 2 显示微信 / 私信聊天框。
4. `/play` 节点 3 显示微信已读不回 / typing 消失效果。
5. `/play` 节点 4 显示文档批注界面。
6. `/play` 节点 5 显示日程邀请 + 倒计时。
7. `/play` 节点 6 显示群聊传言流。
8. 三个选择变成行动卡牌，不再像问卷按钮。
9. 点击选择后出现后果结算浮层。
10. 背景不再是纯黑，而是有网格、噪点、光区、浮动线索。
11. 关系图谱仍然存在，但更动态、更清晰。
12. 结局页有结算动画和逐步 reveal。
13. 移动端不能崩，至少至少能正常阅读和点击。
14. `npm run build` 必须通过。

---

## 十三、重要限制

不要做以下事情：

1. 不要改坏现有游戏逻辑。
2. 不要改坏路由。
3. 不要删除正式剧情数据。
4. 不要加入真实多人在线。
5. 不要加入数据库。
6. 不要接真实 AI。
7. 不要依赖外部图片。
8. 不要只做颜色微调。
9. 不要再返回“已优化暗黑风格”这种抽象结果，必须实现具体场景组件。

完成后请告诉我：

1. 修改了哪些文件。
2. 新增了哪些组件。
3. 每个节点对应哪个场景组件。
4. 如何本地查看效果。
5. `npm run build` 是否通过。
```

---

