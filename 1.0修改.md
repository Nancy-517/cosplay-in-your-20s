
# 任务：重构第一版 UI/UX，让项目从“问卷感”升级为“场景化人生剧本杀体验”

## 背景

当前项目已经完成 Next.js + TypeScript MVP，页面、组件、数据层、逻辑层和 mock API 都已经跑通。

但当前第一版存在明显问题：

1. 页面像静态问卷，游戏感和剧本杀感不足。
2. 整体只是暗黑色，设计层次不够，没有具体视觉意象。
3. 左上角地图名“保研 / 考研：信息差之战”太小。
4. 英文大标题 `PRESSURE MOVES IN LAYERS` 太大，抢占视觉重点。
5. 右侧“多方动态”上方总结文字和下方 NPC 卡片信息重复。
6. 多方博弈没有被可视化，只是文字排列。
7. 结局页直接出现结果，缺少神秘感和结算仪式。

请进行一次 UI/UX 重构，但不要破坏当前已跑通的游戏逻辑、数据结构、路由和 build。

---

## 一、整体设计方向

请把当前风格从“普通暗色问卷”升级为：

**黑色档案系统 + 校园信息战 + 电脑界面 + 关系图谱 + 剧本杀结算感**

关键词：

- 信息差
- 档案
- 线索
- 群聊
- 邮件
- 资料文件
- 关系网络
- 多方博弈
- 隐藏代价
- 平行人生

视觉上不要只是黑色背景。请增加：

1. 细网格背景。
2. 微弱噪点纹理。
3. 冷蓝 / 暗红 / 琥珀色的辅助光源。
4. 卡片边缘微光。
5. 背景中若隐若现的关系线。
6. 节点切换时的动态过渡。

如果不需要额外依赖，优先使用 CSS animation 实现动画。不要为了动画引入过重依赖。如果你认为需要 `framer-motion`，请先说明原因，并保证 `npm run build` 可以通过。

---

## 二、标题层级调整

当前 `PRESSURE MOVES IN LAYERS` 太大，地图名太小。

请修改为：

1. 左上角地图信息放大，清楚显示：
   - 当前地图
   - 保研 / 考研：信息差之战
   - 节点 X / 6

2. `PRESSURE MOVES IN LAYERS` 缩小 35% - 45%，作为氛围标题，而不是主视觉压迫全屏。

3. 节点进度请做成更清晰的进度条或节点胶囊，例如：
   - `节点 1 / 6`
   - 一个横向 progress bar
   - 当前节点高亮

4. 当前剧情标题要更突出，例如：
   - 那份资料
   - 模糊暗示
   - 盟友沉默
   - 体面求助
   - 最后入口
   - 结果前夜

---

## 三、把 6 个剧情节点改成“场景化 UI”

当前所有节点都是同一种文字卡片，太像问卷。请根据不同节点生成不同的场景化视觉组件。

可以新增一个组件：

- `src/components/SceneFrame.tsx`

根据当前 node id 渲染不同的 scene variant。

### 节点 1：那份资料

视觉形式：

- 档案 / PDF 文件界面
- 一张资料卡片从上方轻微掉落
- 文件名可以显示为：`往届面试资料_未公开.pdf`
- 可以有标签：`高频问题`、`导师偏好`、`非公开提醒`

动画：

- 文件卡片 drop-in
- 线索标签 stagger reveal

### 节点 2：模糊暗示

视觉形式：

- 邮件 / 私信界面
- 显示来自“沈砚”的消息
- 消息内容突出：“今年名额可能会比较紧……”

动画：

- 消息气泡 slide-in
- 光标闪烁
- “名额可能会比较紧”做轻微高亮

### 节点 3：盟友沉默

视觉形式：

- 群聊 / 私聊界面
- 显示几条历史消息
- 显示“对方正在输入……”然后消失
- 突出已读不回、回复变慢

动画：

- 聊天气泡逐条出现
- typing indicator 出现后消失

### 节点 4：体面求助

视觉形式：

- 文档批注界面
- 显示“个人陈述.docx”
- 页面上出现几条批注线
- 光标悬停在竞争者材料上

动画：

- 批注线逐渐出现
- 文档边缘扫描光

### 节点 5：最后入口

视觉形式：

- 日程 / 倒计时 / 门禁界面
- 显示“导师空闲窗口：明日 15:30”
- 显示“剩余名额 / 剩余窗口不确定”

动画：

- 倒计时闪烁
- 门缝光效 / access window pulse

### 节点 6：结果前夜

视觉形式：

- 群聊传言流 / 信息噪声界面
- 多条模糊消息快速浮现
- 关键词如“倾向人选”“名额更少”“早就联系过”若隐若现

动画：

- 消息流滚动
- 噪声抖动
- 部分文字 glitch

注意：这些场景不需要真实图片，可以用 CSS、HTML、SVG、卡片、线条和文字做视觉模拟。不要引入外部图片依赖。

---

## 四、选择按钮改成“行动卡”，不要像问卷选项

当前选项像表单按钮。请改成 Action Cards。

每个选项卡至少包含：

1. 行动标题，也就是 choice.text。
2. 一句短描述，可以从 feedback 中截取或自动生成。
3. 风险标签，例如：
   - 信息差
   - 信任变化
   - 关系风险
   - 边界代价
   - 资源收益

如果当前数据结构没有 riskTag，可以在前端根据 effects 自动判断：

- resource 增加明显：资源收益
- trust 下降明显：信任风险
- boundary 下降明显：边界代价
- anxiety 增加明显：压力上升
- reputation 增加明显：声望收益

点击选择后不要立刻进入下一节点。

请保留当前逻辑，但交互改成：

1. 用户点击行动卡。
2. 页面出现“选择后果浮层”。
3. 显示：
   - 你选择了什么
   - feedback 文案
   - 五维变量变化
   - NPC 当前反应
4. 用户点击“继续”后进入下一节点。

选择后果浮层需要有 reveal 动画。

---

## 五、重构右侧“多方动态”：删除重复文字，改为关系图谱

当前右侧多方动态有重复：

- 上方一整段总结文字
- 下方 NPC 卡片

请删除上方重复总结文字。

改成以下结构：

### 1. Relationship Graph 关系图谱

可以新增或重构组件：

- `src/components/RelationshipGraph.tsx`

视觉要求：

- 中间节点是“你”
- 周围四个节点：
  - 林亦然 / 盟友 A
  - 周启明 / 竞争者 B
  - 许知夏 / 消息中枢 C
  - 沈砚 / 导师关系人 D

用 SVG 或 CSS 线条连接。

连线颜色根据 attitudeToPlayer 变化：

- 70 以上：青绿色，表示信任 / 回温
- 40 - 70：蓝灰色，表示观望 / 稳定
- 20 - 40：橙色，表示警惕 / 微妙
- 20 以下：红色，表示敌意 / 破裂

连线粗细或亮度也可以根据 attitudeToPlayer 变化。

每次选择后，关系线需要有 pulse 动画，提示关系发生变化。

### 2. 本轮行动 Timeline

在关系图谱下面展示 NPC 当前行动，不要重复一大段长文字。

格式示例：

```txt
林亦然：关系回温，但开始观察你的信息边界。
周启明：加快了自己的节奏。
许知夏：对你们组合的稳定性产生兴趣。
沈砚：更愿意回应表现稳定的人。
```

请控制每条字数，避免卡片太长。

### 3. 局势提示

根据当前最高 / 最低变量或 NPC 态度，生成一句局势提示。

例如：

```txt
局势提示：你和林亦然的联盟正在修复，但周启明可能已经把你们视为联合行动体。
```

如果没有现成逻辑，可以先写一个简单函数，根据 trust、resource、boundary、anxiety 生成提示。

---

## 六、数值状态卡增强

当前五维状态卡可以保留，但请增强动态反馈：

1. 数值变化时短暂闪烁。
2. 增加或减少时显示 `+5` / `-8` 的小浮动提示。
3. anxiety 焦虑值高于 75 时，状态卡出现轻微橙色警示光。
4. boundary 边界低于 30 时，状态卡出现红色警示光。
5. resource 高于 70 时，资源卡出现冷蓝色高亮。

不要让数值只是静态显示。

---

## 七、结局页增加“结算中”动态效果

当前结局页结果直接出现，缺少神秘感。

请新增结算动画组件：

- `src/components/SettlementLoading.tsx`

进入 `/result` 后，先展示 2.5 - 4 秒结算过程，再展示结果卡。

结算过程文案可以按阶段出现：

1. 正在回收你的选择记录...
2. 正在重建关系网络...
3. 正在计算隐藏代价...
4. 正在生成平行人生...

视觉效果建议：

- 扫描线
- 关系图谱重组
- 进度条
- 五维变量快速回放
- 微弱 glitch 效果
- “最终结算生成中”字样

结算完成后，结果页不要一次性全部出现。

请让结果内容逐项 reveal：

1. 最终结算
2. 结局名称
3. 人格标签
4. 关系结算
5. 隐藏代价
6. 平行人生

每一项间隔 150 - 300ms 出现即可。

---

## 八、页面布局优化

当前页面左右布局太像后台面板。

请优化为更有游戏感的布局：

### 桌面端

建议结构：

```txt
顶部：地图信息 + 节点进度
中部：氛围标题 / 当前节点标题
主体左侧：场景化剧情界面 + 行动卡
主体右侧：关系图谱 + 本轮行动 + 局势提示
底部或浮层：选择后果
```

### 移动端

请保证响应式：

```txt
地图信息
节点进度
剧情场景
行动卡
关系图谱
NPC 动态
```

不要让横向大标题在移动端撑爆页面。

---

## 九、文件修改建议

你可以根据现有结构修改，但建议涉及以下文件：

- `src/app/play/PlayClient.tsx`
- `src/app/result/ResultClient.tsx`
- `src/components/StatusBar.tsx`
- `src/components/RelationshipPanel.tsx`
- `src/components/ChoiceButton.tsx`
- `src/components/ResultCard.tsx`
- `src/app/globals.css`

建议新增组件：

- `src/components/SceneFrame.tsx`
- `src/components/ActionCard.tsx`
- `src/components/ChoiceConsequenceModal.tsx`
- `src/components/RelationshipGraph.tsx`
- `src/components/SettlementLoading.tsx`
- `src/components/SituationHint.tsx`

如果已有类似组件，可以重构，不一定必须新增全部文件。

---

## 十、重要限制

请不要做以下事情：

1. 不要改坏现有路由。
2. 不要删除现有剧情数据。
3. 不要引入真实多人在线。
4. 不要加入数据库。
5. 不要接真实 AI。
6. 不要依赖外部图片资源。
7. 不要把页面做成纯炫技，必须保证信息清晰可读。
8. 不要破坏 `npm run build`。

---

## 十一、验收标准

修改完成后，请确认：

1. `npm run build` 通过。
2. `/play` 页面不再像问卷。
3. 地图名明显变大。
4. `PRESSURE MOVES IN LAYERS` 明显变小。
5. 6 个节点有不同场景化视觉。
6. 选项变成行动卡。
7. 点击选择后先出现选择后果浮层，再进入下一节点。
8. 右侧多方动态不再重复。
9. 多方关系通过图谱可视化。
10. 数值变化有动态反馈。
11. 结局页先显示结算中动画，再逐项 reveal 结果。
12. 页面在桌面端和移动端都能正常阅读。
```

