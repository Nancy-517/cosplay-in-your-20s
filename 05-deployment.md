这个文件是给技术同学和未来的你看的。
这个项目怎么跑？怎么检查？怎么上线？需要哪些配置？

# 05 Deployment Guide

## 1. 项目运行环境

本项目需要 Node.js 和 npm。

如果本地没有安装，需要先安装 Node.js。

安装 Node.js 后，npm 会一起被安装。

## 2. 安装依赖

在项目根目录运行：

```bash
npm install
```
## 3. 本地启动

在项目根目录运行：

```bash
npm run dev
```

启动后，终端通常会显示：

```txt
http://localhost:3000
```

在浏览器中打开这个地址，即可本地预览项目。

## 4. 构建检查

部署前运行：

```bash
npm run build
```

如果没有报错，说明项目可以进入部署流程。

## 5. 环境变量

如果后续接入真实 AI，需要配置环境变量。

参考 `.env.example` 文件。

示例：

```txt
AI_API_KEY=your_api_key_here
AI_BASE_URL=your_ai_base_url_here
```

注意：不要把真实密钥提交到代码仓库。

## 6. 第一版部署前检查清单

部署前需要检查：

1. 首页可以打开。
2. 地图选择页可以打开。
3. 保研 / 考研地图可以进入。
4. 剧情节点可以点击。
5. 玩家状态可以变化。
6. NPC 动态可以展示。
7. 结局页可以生成。
8. 手机浏览器能正常查看。
9. `npm run build` 可以通过。
10. 没有明显控制台报错。

## 7. 当前版本说明

第一版不做真实多人在线。

第一版不做账号登录。

第一版不做复杂数据库。

第一版 AI 接口可以先使用 mock 数据。

## 8. 技术同学需要重点检查

1. 项目结构是否合理。
2. API 接口预留是否方便后续接 AI。
3. 环境变量是否安全。
4. 部署平台是否支持当前项目结构。
5. 构建是否能通过。

